@using Personal_Project_Demo.Models
@model List<TblFeature>
@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<h2>Özellik Listesi</h2>

<table class="table table-bordered">
    <tr>
        <th>#</th>
        <th>Başlık</th>
        <th>Logo</th>
        <th>Açıklama</th>
        <th>Sil</th>
        <th>Güncelle</th>

    </tr>
    @foreach (var item in Model)
    {
        <tr>
            <td>@item.FeatureID</td>
            <td>@item.FeatureTitle</td>
            <td>@item.FeatureLogo</td>
            <td>@item.FeatureDescription</td>
            <td><a href="/Feature/DeleteFeature/@item.FeatureID" class="btn btn-danger">Özellik Sil</a></td>
            <td><a href="/Feature/EditFeature/@item.FeatureID" class="btn btn-success">Özellik Güncelle</a></td>
        </tr>
    }
</table>
<a href="/Feature/AddFeature/" class="btn btn-primary">Yeni Özellik Ekle</a>